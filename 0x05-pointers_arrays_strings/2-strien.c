#include "main.h"

/**
 * _strien - function that returns the length of a string
 * @s: character
 * Return: Always 0.
 */

int _strien(char *s)
{
	int i;

	for (i=0; s[i] != '\0'; i++)
	{
		i = i;
	}
	return (i);
}

